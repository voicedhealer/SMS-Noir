<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Sans ça, le build de release n'a AUCUN accès réseau : le modèle Flutter
         ne déclare INTERNET que dans les manifests debug et profile, pour l'outil
         de développement. L'app y perd tout — get-state, médias, moment IA. -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Notification locale de déblocage de chapitre — voir
         services/notifications_locales.dart. POST_NOTIFICATIONS est le prompt
         runtime Android 13+ ; le reste permet au rappel de survivre à un
         redémarrage du téléphone pendant l'attente (jusqu'à plusieurs heures). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <!-- usesCleartextTraffic : les lecteurs NATIFS (just_audio/ExoPlayer pour
         les musiques et vocaux, video_player pour la transition N9) appliquent
         la politique réseau d'Android, qui interdit le HTTP non chiffré par
         défaut — contrairement au client HTTP de Dart (API, images), qui ne
         l'applique pas. Sans ce flag, AUCUN média audio/vidéo ne peut se lire
         depuis le Supabase local de développement (http://...), alors que tout
         le reste de l'app fonctionne — panne silencieuse trouvée le 22 août
         2026 (« Source error », voir MEMOIRE.md). Le Supabase hébergé de
         production est en https : ce flag ne l'affecte pas, mais à réévaluer
         avant une sortie sur les stores (voir TODO.md). iOS n'a pas ce
         problème : l'ATS d'Apple exempte les médias AVFoundation. -->
    <application
        android:label="SMS Noir"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Requis par flutter_local_notifications pour reprogrammer les
             rappels après un redémarrage — sans ça, une notification
             programmée avant que le téléphone s'éteigne disparaît. -->
        <receiver android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
        <receiver android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
